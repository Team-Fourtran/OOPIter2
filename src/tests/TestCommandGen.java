package tests;

import controllers.*;

import java.util.HashMap;

public class TestCommandGen{
    public static void main(String[] args){

        /*
        Waiting on AssetIterator to test Message Generation
        KeyPressInformer keysim = new KeyPressInformer(new HashMap<String, Boolean>());
        HashMap<String, AssetIterator> = new HashMap<>();
        KeyboardController kbc = new KeyboardController(keySim, );
        */
    }

}